// Placeholder home page — exists only to prove the deploy pipeline (FOUNDATION-PLAN step 1).
// The real contact card replaces this in later steps.
export default function Home() {
  return (
    <main className="flex flex-1 flex-col items-center justify-center gap-3 p-8 text-center">
      <h1 className="text-2xl font-semibold tracking-tight">Contact Card</h1>
      <p className="text-sm text-foreground/60">
        Foundation deployed. Building the card&hellip;
      </p>
    </main>
  );
}
